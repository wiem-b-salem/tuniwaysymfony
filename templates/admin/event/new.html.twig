{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <h1 class="h3 mb-4 fw-bold text-primary">Create New Event</h1>

                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <div class="mt-4 d-flex justify-content-between">
                            <a href="{{ path('app_admin_event_index') }}" class="btn btn-outline-secondary">Back to List</a>
                            <button class="btn btn-primary">{{ button_label|default('Save Event') }}</button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
